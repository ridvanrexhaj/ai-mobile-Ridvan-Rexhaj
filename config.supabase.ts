// 🔧 CONFIGURATION FILE - Edit these values for Expo Snacks
// On Replit: These come from Secrets automatically
// On Snacks: Edit these values directly with your Supabase credentials

export const supabaseConfig = {
  // Get these from your Supabase project settings
  url: process.env.EXPO_PUBLIC_SUPABASE_URL || 'https://your-project.supabase.co',
  anonKey: process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY || 'your-anon-key-here',
};
